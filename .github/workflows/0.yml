name: CI_action

on:
  push:
    branches: [main, development]
  pull_request:
    branches: [main, development]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          ref: development

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm install
        working-directory: worksheet2—my-react-app  # 使用中文破折号

      - name: Run the tests
        run: npm test
        working-directory: worksheet2—my-react-app  # 使用中文破折号

      - name: Build
        run: npm run build
        working-directory: worksheet2—my-react-app  # 使用中文破折号
